"""
User-interface functions for several commonly needed tasks.

Each module contains optparse-based function 'interface' that can be used as
such. By default, the function reads the command-line arguments from
sys.argv[1:] but arguments can also be manually specified.
"""
